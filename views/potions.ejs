<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <%- include("./partials/navbar", {links}) %> <%-
    include("./partials/addPotion") %>
    <h1><%= title %></h1>
    <ul>
      <% potions.forEach((item) => { %>
      <li>
        id: <%= item.shop_inventory_id %>; name: <%= item.name %>; potion type:
        <%= item.potion_type_id %>; effect: <%= item.effect %>; duration: <%=
        item.duration %>;
        <a href="/<%= item.id %>/update">Update</a>
        <form action="/<%= item.id %>/delete" method="POST">
          <button
            type="submit"
            onclick="return confirm('Are you sure this item no longer belongs?');"
          >
            Delete
          </button>
        </form>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
