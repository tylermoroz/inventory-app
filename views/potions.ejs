<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <%- include("./partials/navbar", {itemTypes, weaponTypes, spellTypes}) %> <%-
    include("./partials/addPotion", {potionTypes}) %>
    <h1><%= title %></h1>
    <ul>
      <% potions.forEach((potion) => { %>
      <li>
        id: <%= potion.shop_inventory_id %>; name: <%= potion.name %>; potion
        type: <%= potionTypes.find(type => type.id ===
        potion.potion_type_id).name %>; duration(seconds): <%=
        potion.duration_seconds %>;
        <a href="/potions/<%= potion.shop_inventory_id %>/update">Update</a>
        <form action="/<%= potion.shop_inventory_id %>/delete" method="POST">
          <button
            type="submit"
            onclick="return confirm('Are you sure this potion no longer belongs?');"
          >
            Delete
          </button>
        </form>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
