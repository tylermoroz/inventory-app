<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <%- include("./partials/navbar", {itemTypes}) %> <%-
    include("./partials/addWeapon", {weaponTypes, affinityTypes}) %>
    <h1><%= title %></h1>
    <ul>
      <% weapons.forEach((weapon) => { %>
      <li>
        id: <%= weapon.shop_inventory_id %>; name: <%= weapon.name %>; weapon
        type: <%= weaponTypes.find(type => type.id ===
        weapon.weapon_type_id).name %>; damage: <%= weapon.damage %>;
        durability: <%= weapon.durability %>; affinity: <%=
        affinityTypes.find(affinity => affinity.id ===
        weapon.affinity_type_id).name %>;
        <a href="/weapons/<%= weapon.shop_inventory_id %>/update">Update</a>
        <form action="/<%= weapon.shop_inventory_id %>/delete" method="POST">
          <button
            type="submit"
            onclick="return confirm('Are you sure this weapon no longer belongs?');"
          >
            Delete
          </button>
        </form>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
