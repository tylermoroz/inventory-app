<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
</head>
<body>
    <div class="update-item-container">
        <h1><%= title %></h1>
        <%- include("../partials/errors") %>
        <div class="form-container">
            <form action="/potions/<%= potion.id %>/update" method="POST" id="updateForm">
                <div class="parent-input-container">
                    <div class="name-input-container">
                        <label for="name">Name: </label>
                        <input type="text" name="name" id="name" value="<%= potion.name %>">
                    </div>
                    <div class="potion-type-input-container">
                        <label for="potion_type_id">Potion Type: </label>
                        <select name="potion_type_id" id="potion_type_id">
                            <% potionTypes.forEach((type) => { %>
                            <option value="<%= type.id %>" <%= type.id === potion.potion_type_id ? "selected" : "" %>
                            >
                            <%= type.name %>
                            </option>
                            <% }) %>
                        </select>
                    </div>
                </div>
                <div class="slider-container">
                    <label for="duration_seconds">Duration(seconds): </label>
                    <input class="stat-slider" type="range" name="duration_seconds" id="duration_seconds" min="1" max="20" value="<%= potion.duration_seconds %>" />
                    <span class="slider-value"><%= potion.duration_seconds %></span>
                </div>
                <div class="slider-container">
                    <label for="weight">Weight: </label>
                    <input class="stat-slider" type="range" name="weight" id="weight" min="1" max="100" value="<%= potion.weight %>" />
                    <span class="slider-value"><%= potion.weight %></span>
                </div>
                <div class="value-input-container">
                    <label for="value">Value: </label>
                    <input type="number" name="value" id="value" min="1" max="1000" value="<%= potion.value %>" />
                </div>
                <div class="submit-button-container">
                    <button type="submit">Update Potion</button>
                </div>
            </form>
            <a href="/potions">Back to Potions</a>
        </div>
    </div>
    <script src="/scripts/sliders.js"></script>
    <script src="/scripts/secureUpdate.js"></script>
</body>
</html>